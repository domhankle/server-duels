[tool.poetry]
name = "server-duels"
version = "0.2"
description = "A Discord bot to duel your friends"
authors = ["Domenic Hankle", "Victor Londono"]
packages = [{include = "server-duels"}]

[tool.poetry.dependencies]
python = "^3.10"
discord = "^2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
